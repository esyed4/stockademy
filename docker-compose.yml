version: '3.4'

services:
  web:
    image: web
    build: .
    volumes:
      - .:/app
      - /app/node_modules
    tty: true
    stdin_open: true
    ports:
      - "3000:3000"
    expose:
      - 3000
    command: bash -c "rm -f tmp/pids/server.pid && bin/dev"

